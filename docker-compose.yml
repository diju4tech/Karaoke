version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - KARAOKE_DATA=/data
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 4g
